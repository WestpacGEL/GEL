/** @jsx jsx */

import { jsx } from '@westpac/core';
import React, { Fragment } from 'react';

export const Wrapper = ({ children }) => <Fragment children={children} />;

export const wrapperStyles = () => ({});
