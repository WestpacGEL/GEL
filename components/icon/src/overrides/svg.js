/** @jsx jsx */

import { jsx } from '@westpac/core';
import React from 'react';

export const Svg = ({ color, size, assistiveText, ...rest }) => <svg {...rest} />;

export const svgStyles = () => ({});
