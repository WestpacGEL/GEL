/** @jsx jsx */

import { jsx } from '@westpac/core';
import React, { Fragment } from 'react';

export const Content = ({ children }) => <Fragment children={children} />;

export const contentStyles = () => ({});
