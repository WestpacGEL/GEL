/** @jsx jsx */

import { jsx } from '@westpac/core';

export const ToggleTextWrapper = ({ children }) => <Fragment>{children}</Fragment>;

export const toggleTextWrapperStyles = (_, {}) => {
	return {};
};
