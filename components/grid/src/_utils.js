export const containerMap = {
	paddingHorizontal: [12, 30, 36, 48, 60],
	maxWidthFixed: [null, null, 768, 992, 1200],
	maxWidthFluid: 1320, //1200 (lg) + 60 (paddingHorizontal) + 60 (paddingHorizontal)
};

export const gridMap = {
	columns: 12,
	gap: [12, 24],
};
