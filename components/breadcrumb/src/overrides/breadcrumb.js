/** @jsx jsx */

import { jsx } from '@westpac/core';
import React from 'react';

export const Breadcrumb = ({ assistiveText, currentAssistiveText, ...props }) => <div {...props} />;

export const breadcrumbStyles = () => ({});
