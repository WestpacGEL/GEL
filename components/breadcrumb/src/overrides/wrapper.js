/** @jsx jsx */

import { jsx } from '@westpac/core';
import React from 'react';

export const Wrapper = ({ assistiveText, currentAssistiveText, ...props }) => <div {...props} />;

export const wrapperStyles = () => ({});
